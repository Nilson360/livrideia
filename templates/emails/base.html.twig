<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Livridea{% endblock %}</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            line-height: 1.6;
            color: #374151;
            background-color: #f9fafb;
        }

        .email-container {
            max-width: 600px;
            margin: 0 auto;
            background-color: #ffffff;
            box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
        }

        .header {
            background: linear-gradient(135deg, #FF8A00, #e67300);
            color: white;
            padding: 30px 20px;
            text-align: center;
        }

        .header h1 {
            font-size: 28px;
            font-weight: bold;
            margin-bottom: 8px;
        }

        .header .tagline {
            font-size: 14px;
            opacity: 0.9;
        }

        .content {
            padding: 40px 30px;
            background: #ffffff;
        }

        .content h2 {
            color: #1f2937;
            font-size: 24px;
            margin-bottom: 20px;
            font-weight: 600;
        }

        .content p {
            margin-bottom: 16px;
            color: #4b5563;
            font-size: 16px;
        }

        .content ul {
            margin: 20px 0;
            padding-left: 20px;
        }

        .content li {
            margin-bottom: 8px;
            color: #4b5563;
        }

        .btn {
            display: inline-block;
            background: linear-gradient(135deg, #FF8A00, #e67300);
            color: white;
            padding: 14px 28px;
            text-decoration: none;
            border-radius: 8px;
            font-weight: 600;
            font-size: 16px;
            text-align: center;
            transition: all 0.3s ease;
            box-shadow: 0 2px 4px rgba(255, 138, 0, 0.2);
        }

        .btn:hover {
            background: linear-gradient(135deg, #e67300, #cc6600);
            transform: translateY(-1px);
            box-shadow: 0 4px 8px rgba(255, 138, 0, 0.3);
        }

        .btn-secondary {
            background: #f3f4f6;
            color: #374151;
            border: 1px solid #d1d5db;
        }

        .btn-secondary:hover {
            background: #e5e7eb;
            transform: translateY(-1px);
        }

        .book-card {
            background: #f9fafb;
            border: 1px solid #e5e7eb;
            border-radius: 12px;
            padding: 20px;
            margin: 16px 0;
            display: flex;
            gap: 16px;
        }

        .book-cover {
            width: 80px;
            height: 120px;
            background: #d1d5db;
            border-radius: 6px;
            flex-shrink: 0;
        }

        .book-cover img {
            width: 100%;
            height: 100%;
            object-fit: cover;
            border-radius: 6px;
        }

        .book-info h3 {
            color: #1f2937;
            font-size: 18px;
            font-weight: 600;
            margin-bottom: 4px;
        }

        .book-info .author {
            color: #FF8A00;
            font-weight: 500;
            margin-bottom: 8px;
        }

        .book-info .description {
            color: #6b7280;
            font-size: 14px;
            line-height: 1.5;
        }

        .highlight-box {
            background: linear-gradient(135deg, #fef3e2, #fed7aa);
            border-left: 4px solid #FF8A00;
            padding: 20px;
            margin: 24px 0;
            border-radius: 0 8px 8px 0;
        }

        .highlight-box h3 {
            color: #92400e;
            margin-bottom: 8px;
        }

        .highlight-box p {
            color: #92400e;
            margin: 0;
        }

        .stats {
            display: flex;
            justify-content: space-around;
            background: #f3f4f6;
            padding: 20px;
            border-radius: 8px;
            margin: 20px 0;
        }

        .stat {
            text-align: center;
        }

        .stat-number {
            font-size: 24px;
            font-weight: bold;
            color: #FF8A00;
        }

        .stat-label {
            font-size: 12px;
            color: #6b7280;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .footer {
            background: #1f2937;
            color: #d1d5db;
            padding: 30px 20px;
            text-align: center;
        }

        .footer-links {
            margin-bottom: 20px;
        }

        .footer-links a {
            color: #FF8A00;
            text-decoration: none;
            margin: 0 10px;
            font-size: 14px;
        }

        .footer-links a:hover {
            text-decoration: underline;
        }

        .footer p {
            font-size: 12px;
            color: #9ca3af;
            margin: 8px 0;
        }

        .social-links {
            margin: 20px 0;
        }

        .social-links a {
            display: inline-block;
            margin: 0 8px;
            color: #FF8A00;
            font-size: 18px;
        }

        @media only screen and (max-width: 600px) {
            .email-container {
                margin: 0;
                border-radius: 0;
            }

            .content {
                padding: 30px 20px;
            }

            .book-card {
                flex-direction: column;
                text-align: center;
            }

            .book-cover {
                align-self: center;
            }

            .stats {
                flex-direction: column;
                gap: 16px;
            }
        }
    </style>
</head>
<body>
<div class="email-container">
    <div class="header">
        <h1>📚 Livridea</h1>
        <div class="tagline">Votre passion littéraire, notre inspiration</div>
    </div>

    <div class="content">
        {% block content %}{% endblock %}
    </div>

    <div class="footer">
        <div class="footer-links">
            <a href="{{ siteUrl|default('https://livridea.com') }}">Accueil</a>
            <a href="{{ discoverUrl|default('https://livridea.com/decouvrir') }}">Découvrir</a>
            <a href="{{ profileUrl|default('https://livridea.com/profil') }}">Mon Profil</a>
            {% if settingsUrl is defined %}
                <a href="{{ settingsUrl }}">Paramètres</a>
            {% endif %}
        </div>

        <div class="social-links">
            <a href="#" title="Facebook">📘</a>
            <a href="#" title="Twitter">🐦</a>
            <a href="#" title="Instagram">📷</a>
        </div>

        <p>© {{ 'now'|date('Y') }} Livridea. Tous droits réservés.</p>
        <p>
            {% if unsubscribeUrl is defined %}
                <a href="{{ unsubscribeUrl }}" style="color: #9ca3af;">Se désabonner</a> |
            {% endif %}
            <a href="mailto:contact@livridea.com" style="color: #9ca3af;">Nous contacter</a>
        </p>
    </div>
</div>
</body>
</html>