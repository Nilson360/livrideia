<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{% block title %}Livridea{% endblock %}</title>
    <link rel="icon" href="/images/book-icon.svg">
    <link href="https://fonts.googleapis.com/css2?family=Merriweather:wght@400;700&display=swap" rel="stylesheet">
    {% block stylesheets %}
        {{ encore_entry_link_tags('app') }}
    {% endblock %}
</head>
<body class="bg-gray-50 text-gray-900 font-serif min-h-screen flex flex-col">
<header class="py-4 px-4 flex justify-center border-b">
    <div class="text-center">
        <h1 class="text-2xl font-bold text-[#FF8A00] italic">Livridea</h1>
    </div>
</header>

<main class="flex-1 flex flex-col p-4">
    {% block flash_messages %}
        {% for type, messages in app.flashes %}
            {% for message in messages %}
                <div class="mb-4 p-3 rounded-lg {% if type == 'error' or type == 'danger' %}bg-red-500 text-white{% elseif type == 'success' %}bg-green-500 text-white{% else %}bg-blue-500 text-white{% endif %}">
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}
    {% endblock %}

    {% block content %}{% endblock %}
</main>

{% block javascripts %}
    {{ encore_entry_script_tags('app') }}
{% endblock %}
</body>
</html>