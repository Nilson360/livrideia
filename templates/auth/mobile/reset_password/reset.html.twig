{% extends 'auth/mobile/base_auth_mobile.html.twig' %}

{% block title %}Nouveau mot de passe{% endblock %}

{% block content %}
    <div class="flex-1 flex flex-col justify-center items-center">
        <div class="w-full max-w-md">
            <div class="bg-white rounded-2xl shadow-xl border border-gray-100 overflow-hidden">
                <div class="p-8">
                    <div class="text-center mb-8">
                        <div class="w-16 h-16 bg-gradient-to-br from-emerald-500 to-emerald-600 rounded-full flex items-center justify-center mx-auto mb-4 shadow-lg">
                            <svg class="w-8 h-8 text-white" viewBox="0 0 24 24" fill="currentColor">
                                <path d="M12 1L3 5v6c0 5.55 3.84 10.74 9 12 5.16-1.26 9-6.45 9-12V5l-9-4z"/>
                            </svg>
                        </div>
                        <h2 class="text-2xl font-bold text-gray-800 mb-2">Nouveau mot de passe</h2>
                        <p class="text-gray-500">Choisissez un mot de passe sécurisé pour votre compte.</p>
                    </div>

                    {# Affichage des erreurs globales du formulaire #}
                    {% for error in resetForm.vars.errors %}
                        <div class="bg-red-500 text-white text-sm p-2 rounded mb-2">{{ error.message }}</div>
                    {% endfor %}

                    {{ form_start(resetForm, {'attr': {'class': 'space-y-6', 'id': 'new-password-form'}}) }}

                    <!-- Nouveau mot de passe -->
                    <div>
                        <label for="{{ resetForm.plainPassword.first.vars.id }}" class="block text-gray-700 font-semibold mb-2">Nouveau mot de passe</label>
                        <div class="relative">
                            {{ form_widget(resetForm.plainPassword.first, {
                                'attr': {
                                    'class': 'w-full border-2 border-gray-200 rounded-xl p-4 focus:outline-none focus:border-emerald-500 transition-colors duration-200 pl-12 pr-12',
                                    'placeholder': 'Saisissez un mot de passe sécurisé'
                                }
                            }) }}
                            <svg class="w-5 h-5 text-gray-400 absolute left-4 top-1/2 transform -translate-y-1/2"
                                 viewBox="0 0 24 24" fill="currentColor">
                                <path d="M18 8h-1V6c0-2.76-2.24-5-5-5S7 3.24 7 6v2H6c-1.1 0-2 .9-2 2v10c0 1.1.9 2 2 2h12c1.1 0 2-.9 2-2V10c0-1.1-.9-2-2-2zm-6 9c-1.1 0-2-.9-2-2s.9-2 2-2 2 .9 2 2-.9 2-2 2zm3.1-9H8.9V6c0-1.71 1.39-3.1 3.1-3.1 1.71 0 3.1 1.39 3.1 3.1v2z"/>
                            </svg>
                        </div>
                        {{ form_errors(resetForm.plainPassword.first) }}
                    </div>

                    <!-- Confirmation du mot de passe -->
                    <div>
                        <label for="{{ resetForm.plainPassword.second.vars.id }}" class="block text-gray-700 font-semibold mb-2">Répéter le mot de passe</label>
                        <div class="relative">
                            {{ form_widget(resetForm.plainPassword.second, {
                                'attr': {
                                    'class': 'w-full border-2 border-gray-200 rounded-xl p-4 focus:outline-none focus:border-emerald-500 transition-colors duration-200 pl-12 pr-12',
                                    'placeholder': 'Répétez votre mot de passe'
                                }
                            }) }}
                            <svg class="w-5 h-5 text-gray-400 absolute left-4 top-1/2 transform -translate-y-1/2"
                                 viewBox="0 0 24 24" fill="currentColor">
                                <path d="M12 1L3 5v6c0 5.55 3.84 10.74 9 12 5.16-1.26 9-6.45 9-12V5l-9-4z"/>
                            </svg>
                        </div>
                        {{ form_errors(resetForm.plainPassword.second) }}
                    </div>

                    <!-- Conseils sécurité -->
                    <div class="bg-blue-50 border border-blue-200 rounded-xl p-4 mb-2">
                        <div class="flex items-start">
                            <svg class="w-5 h-5 text-blue-500 mr-3 mt-0.5 flex-shrink-0" viewBox="0 0 24 24" fill="currentColor">
                                <path d="M12 1L3 5v6c0 5.55 3.84 10.74 9 12 5.16-1.26 9-6.45 9-12V5l-9-4z"/>
                            </svg>
                            <div class="text-sm text-blue-700">
                                <p class="font-medium mb-2">Conseils pour un mot de passe sécurisé :</p>
                                <ul class="space-y-1">
                                    <li>• Au moins 8 caractères</li>
                                    <li>• Mélange de majuscules, minuscules, chiffres et caractères spéciaux</li>
                                    <li>• Évitez les informations personnelles</li>
                                </ul>
                            </div>
                        </div>
                    </div>

                    <button type="submit"
                            class="w-full bg-gradient-to-r from-emerald-500 to-emerald-600 text-white py-4 rounded-xl font-semibold shadow-lg active:scale-95 transition-all duration-200 flex items-center justify-center mb-2">
                        <svg class="w-5 h-5 mr-2" viewBox="0 0 24 24" fill="currentColor">
                            <path d="M12 1L3 5v6c0 5.55 3.84 10.74 9 12 5.16-1.26 9-6.45 9-12V5l-9-4z"/>
                        </svg>
                        Réinitialiser le mot de passe
                    </button>
                    {{ form_end(resetForm) }}

                    <div class="mt-6 text-center">
                        <a href="{{ path('app_login') }}" class="text-[#FF8A00] font-semibold active:text-[#e67300] transition-colors text-lg flex items-center justify-center">
                            <svg class="w-4 h-4 mr-2" viewBox="0 0 24 24" fill="currentColor">
                                <path d="M20 11H7.83l5.59-5.59L12 4l-8 8 8 8 1.41-1.41L7.83 13H20v-2z"/>
                            </svg>
                            Retour à la connexion
                        </a>
                    </div>
                </div>
                <div class="bg-emerald-50 px-8 py-6 border-t border-gray-100">
                    <div class="text-center">
                        <p class="text-emerald-700 text-sm font-medium mb-1">🔒 Connexion sécurisée</p>
                        <p class="text-emerald-600 text-xs">Votre mot de passe sera chiffré et protégé</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
